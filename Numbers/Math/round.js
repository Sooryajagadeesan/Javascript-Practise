// Math.round(number) - ROUNDS UP/DOWN a number based on the disgits after the decimal point

let number = 4.1234567

console.log(Math.round(number)) // 4

console.log() // for a new line

console.log(Math.round(10.0)) // NOTE: if 10.0 -> 10
console.log(Math.round(5.7)) // 6
console.log(Math.round(6.1)) // 6 NOTE the digits after decimal point
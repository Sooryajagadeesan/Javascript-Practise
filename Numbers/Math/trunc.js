// Math.trunc(number) - cuts all the digits after the decimal point and returns only the integral digit

let number = 2.1234567

console.log(Math.trunc(number)) // 2

console.log() // for a new line

console.log(Math.trunc(10.0)) // 10
console.log(Math.trunc(5.7)) // 5
console.log(Math.trunc(6.1)) // 6